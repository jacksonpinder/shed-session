fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
whisperx>=3.1.1
librosa>=0.10  # beat tracking + pulse clarity (#4); usually already pulled in transitively
# whisperx pulls in faster-whisper + torch; install a torch build matching your
# CUDA (see https://pytorch.org) for GPU. CPU-only works but is slower.
